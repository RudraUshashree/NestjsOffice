export enum Account_Type {
    Student = "Student",
    Employee = "Employee"
}

export enum Account_status {
    Approval_Pending = "Approval_Pending",
    Active = "Active",
    Inactive = "Inactive",
    Blocked = "Blocked",
    Deleted = "Deleted"
}